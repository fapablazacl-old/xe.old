
#include "Subset.hpp"

#include <algorithm>

namespace xe {
    //bool Mesh::isIndexed() const {
    //    auto fields = this->getFormat().fields;
    //    auto pos = std::find_if(fields.begin(), fields.end(), [](const SubsetAttrib &attrib) {
    //        return attrib.bufferType == BufferType::Index; 
    //    });

    //    return pos != fields.end();
    //}
}
