
#include "Subset.hpp"

#include <algorithm>

namespace xe {
    //bool Mesh::isIndexed() const {
    //    auto attribs = this->getFormat().attribs;
    //    auto pos = std::find_if(attribs.begin(), attribs.end(), [](const SubsetAttrib &attrib) {
    //        return attrib.bufferType == BufferType::Index; 
    //    });

    //    return pos != attribs.end();
    //}
}
